name: Supabase Heartbeat Cron

on:
  schedule:
<<<<<<< HEAD
    - cron: "*/5 * * * *"
=======
    - cron: "* * * * *"
>>>>>>> afec009 ( update cron interval from 5 minutes to 1 minute for testing)
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_API_KEY: ${{ secrets.SUPABASE_API_KEY }}
<<<<<<< HEAD
        run: python keepalive.py
=======
        run: python keepalive.py
>>>>>>> afec009 ( update cron interval from 5 minutes to 1 minute for testing)
